import { Router } from 'express';

/**
 * Configuração central de todas as rotas da aplicação
 * As rotas específicas estão nos arquivos individuais
 */
export function createRoutes(): Router {
  const router = Router();
  
  // As rotas são configuradas diretamente no index.ts
  // Este arquivo serve apenas como placeholder
  
  return router;
}
